---
description: Inspect/watch/manage a Codex Desktop session or start a guarded Claude-to-Codex handoff.
argument-hint: "[conversationId|watch <conversationId>|new] [instruction]"
---

Load `.claude/skills/ipc/SKILL.md` and follow it. Treat these arguments as the `/ipc` invocation:

`$ARGUMENTS`
