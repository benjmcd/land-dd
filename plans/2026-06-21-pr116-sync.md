# Post-PR116 Routing Sync

## Goal
Refresh current routing and state after PR #116 merged `BRC-001`, so the canonical
project surfaces no longer describe the completed Bologna recorded-source corpus
contract as the active implementation lane.

## Non-goals
- Do not change source/report behavior, source readiness, database schema, APIs, UI,
  fixtures, connectors, rulepacks, or release-readiness semantics.
- Do not approve Bologna sources, select a Bologna AOI, capture recorded fixtures,
  promote source registry rows, create source-failure fixtures, or start report use.
- Do not unblock DS-017, hosted platform, identity/RBAC, hosted observability, object
  storage, billing, image publication, secrets, production traffic, or Level 10 status.

## Current state
Live `origin/main` is `4b29bcf646e0cf61bbf3eedee00417a4eed9f115`, which merged PR
#116 (`BRC-001`). The root checkout remains a dirty preserved candidate lane on
`codex/r026-raw-readiness-ui`, so this sync is running from `worktrees/post116-sync`.

`state/PROJECT_STATE.md`, `plans/README.md`, and `tasks/task_queue.yaml` still route to
the completed corpus contract as current work. The corpus contract itself is valid and
blocked as intended; the stale piece is the project routing.

`state/LEVEL_9_10_GATE_MATRIX.md` remains the Level 9/10 authority context for this
sync. Routing updates must not promote hosted, source, production-traffic, or Level 10
proof from repo-local state alone.

## Proposed design
Make a state/routing-only update:

- record PR #116 and merge commit as the latest live checkpoint;
- add this sync plan as the current routing plan;
- add `PR116-SYNC` as a completed queue entry once validation passes;
- preserve every external blocker and the remaining dirty-root residual deferrals.

Rejected alternative: start the next Bologna implementation pass now. That would rely on
repo-local inference where product/AOI/source-review authority is still absent, and would
contradict the existing source-rights and production-authority guards.

## Bottom-up sequence
1. Reconfirm live `origin/main`, worktree layout, and focused baseline validators.
2. Update plan, state, queue, worklog, and validation-log routing.
3. Run focused checks plus workspace validation and full verification.
4. Publish and merge only if CI passes, then revalidate from detached live main and remove
   the worktree.

## Files likely to change

| File | Expected change |
|---|---|
| `plans/2026-06-21-pr116-sync.md` | New routing sync plan |
| `plans/README.md` | Mark `BRC-001` latest completed and this plan current |
| `state/PROJECT_STATE.md` | Record PR #116 as the live checkpoint and preserve blockers |
| `tasks/task_queue.yaml` | Route active plan to this sync and add `PR116-SYNC` |
| `state/WORKLOG.md` | Add concise worklog entry |
| `state/VALIDATION_LOG.md` | Record validation commands/results |
| `plans/2026-06-21-bologna-recorded-source-corpus.md` | Clarify its current-state paragraph as historical |

## Tests / verification
```powershell
py -3.12 .\scripts\bologna_recorded_source_corpus_check.py
py -3.12 .\scripts\bologna_preflight_check.py
py -3.12 .\scripts\release_readiness_check.py
py -3.12 .\scripts\readiness_matrix_check.py
py -3.12 .\scripts\source_readiness.py --priority Must --json
git diff --check
git diff --name-only --diff-filter=D
.\scripts\validate_workspace.ps1
.\scripts\verify.ps1
```

Expected signal: all checks pass, no deleted files are present, and Must-source readiness
remains `sources=8 ready=7 blocked=1` with `DS-017` still blocked.

## Risks and blockers
The main risk is overstating progress. This sync only updates routing after a merge; it
does not create new product, legal, hosted, source, tenant, runtime, or Level 9/10
authority.

## Decision log
- 2026-06-21: Chose a narrow post-PR116 routing sync before any new implementation,
  because canonical state still pointed at already-merged work.

## Progress log
- 2026-06-21: Created `worktrees/post116-sync` from live `origin/main` at
  `4b29bcf646e0cf61bbf3eedee00417a4eed9f115`.
- 2026-06-21: Baseline focused validators passed before edits.
- 2026-06-21: Applied routing/state/log updates and corrected the active plan after
  `readiness_matrix_check.py` required an explicit `state/LEVEL_9_10_GATE_MATRIX.md`
  / Level 9/10 authority citation.
- 2026-06-21: Focused validators, source-readiness proof, diff/no-deletion checks,
  workspace validation, and full `.\scripts\verify.ps1` passed.
