# Plans

Active implementation plans live here. Keep them executable and narrow.

Latest completed plan: `2026-06-20-post-g9a-roadmap-reconciliation.md`.

Active plan: `2026-06-20-generic-aoi-evidence-closure.md`.

The active slice is `G9b` generic supported-AOI evidence-rich workflow closure.
`REC-002` produced `state/residual-reconciliation.md` and found no dirty-root
implementation slice that should be copied forward wholesale. The next unblocked
engineering path is to audit and then prove non-packaged AOIs inside the selected North
Carolina counties through evidence-rich, reviewed, DB-backed reports without relying on
packaged operator-case IDs. It must not select a new jurisdiction, add new source
authority, approve DS-017, start Bologna, create hosted deployment/identity/
observability authority, or claim Level 10 production proof.

Lane 1 routing artifacts remain `state/reconciliation-inventory.md`,
`state/reconciliation-slices.md`, `state/r023-review.md`, and
`state/reconciliation-dispositions.md`. The disposition matrix remains the historical
source for initial retain/rework/defer/archive/discard decisions; current residual
classification is in `state/residual-reconciliation.md`.

Superseded plans should be moved to `plans/archive/` with a note at the top.
